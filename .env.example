# Use a URL-safe value; this is used to construct the internal PostgreSQL URL.
POSTGRES_PASSWORD=replace-with-a-long-random-password

# Optional overrides
POSTGRES_USER=calibration
POSTGRES_DB=calibration_tracker
CALIBRATION_INTERVAL_SECONDS=900
LOG_LEVEL=INFO
